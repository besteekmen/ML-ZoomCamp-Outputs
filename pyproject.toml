[project]
name = "ml-zoomcamp-outputs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Beste Ekmen", email = "besteekmen@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
    "seaborn>=0.13.2",
]

[project.scripts]
ml-zoomcamp-outputs = "ml_zoomcamp_outputs:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
